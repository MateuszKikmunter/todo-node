import { PastDeadlineDirective } from './past-deadline.directive';

describe('PastDeadlineDirective', () => {
  it('should create an instance', () => {
    const directive = new PastDeadlineDirective();
    expect(directive).toBeTruthy();
  });
});
